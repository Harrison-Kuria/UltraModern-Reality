<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ultramodern.activity.milkdiary">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".username_request"></activity>
        <activity android:name=".setting_splash" />
        <activity android:name=".WelcomeSplash" />
        <activity android:name=".Welcome" />
        <activity android:name=".Records_now" />
        <activity android:name=".Billing" />
        <activity android:name=".BillingSplash" />
        <activity android:name=".records" />
        <activity android:name=".settings" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PhoneNumberAuthentication" />
        <activity android:name=".PhoneNumber" />
        <activity android:name=".Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>